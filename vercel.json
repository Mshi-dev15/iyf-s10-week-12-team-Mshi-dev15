{
  "buildCommand": "ls -la && test -d frontend && echo 'Frontend directory exists' || echo 'Frontend directory missing' && cd frontend && ls -la && npm install && npm run build",
  "outputDirectory": "frontend/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
